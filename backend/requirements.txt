# Runtime dependency closure for the backend image (python:3.13 slim) and for local Python >= 3.11.
# Every direct and transitive package is pinned. Resolved 2026-09-06 from PyPI metadata; the
# Dockerfile installs with `pip install --no-deps` and then runs `pip check`, so a missing or
# conflicting pin fails the build instead of pulling an unpinned package.
#
# Direct: fastapi, uvicorn, pymongo (see pyproject.toml). Everything else is transitive.
fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.5
pydantic-core==2.46.5
annotated-types==0.8.0
annotated-doc==0.0.5
typing-extensions==4.16.0
typing-inspection==0.4.4
anyio==4.15.1
idna==3.19
exceptiongroup==1.3.1; python_version < "3.11"
uvicorn==0.52.4
click==8.5.0
h11==0.16.0
pymongo==4.18.0
dnspython==2.8.0
